# Discovery only (replay needs no model):
ANTHROPIC_API_KEY=sk-ant-...
# Operator credentials for the synthetic CoreServ app (resolved by the secret provider, never by the model)
CORESERV_USER=teller01
CORESERV_PASSWORD=demo-only-pw
